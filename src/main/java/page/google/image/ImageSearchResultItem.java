package page.google.image;

import org.openqa.selenium.WebElement;
import page.common.SearchResultBaseItem;

public class ImageSearchResultItem extends SearchResultBaseItem {
    public ImageSearchResultItem(WebElement element) {
        super(element);
    }
}
